<p id="notice"><%= notice %></p>

<nav class="navbar navbar-default">
	<div class="container-fluid">
		<ul class="nav navbar-nav">
			<li><a href='#'>Filters <span class='glyphicon glyphicon-filter'></span></a></li>
		</ul>
		<ul class="nav navbar-nav navbar-right">
			<li>
				<% if current_user %>
								<a>	<%= current_user.email %> </a></li><li> <%= link_to "Log out", logout_path, method: "delete" %></li>
				<% else %>
						<%= link_to "Login <span class='glyphicon glyphicon-user'></span>".html_safe, 'login' %></li>
				<% end %>
			</li>
		</ul>
	</div>
</nav>
<div class="container">
				<% if current_user %>
								<%= link_to new_classroom_path do %>
										<div class='col-md-5'></div>
										<div class='col-md-2 col-sm-12 well CDB_NEW'>New Classroom</div>
										<div class='col-md-5'></div>
										<% end %>
								<% end %>
				</div>
</div>
<h1 style="text-align:center">Listing Classrooms</h1>

		<% if @classrooms.count == 0 %>
			<div class="container">
				<h2 style="text-align: center">No Classrooms are in the database at this time.</h2>
			</div>
		<% else %>
			<div class="container">			
			<% @classrooms.each do |classroom| %>			
							<%= link_to classroom_path(classroom) do %>	
							<div class='wrapper col-sm-3'>
									<%= image_tag classroom.pictures.first.image_url(:thumb).to_s %>
					<div class="CDB_TH_NAME">
						<div class="wrapper-content">
							<span class="glyphicon glyphicon-search"></span>
						</div>
				</div>
				<div class='CDB_NAME'><%= classroom.name %></div>
				</div>
				<% end %>
				<% if current_user %>
				<div class='container'>
        			<%= link_to 'Edit', edit_classroom_path(classroom) %>
        			<%= link_to 'Destroy', classroom, method: :delete, data: { confirm: 'Are you sure?' } %>
				</div>
				<% end %>
				<% end %>
				</div>
		<% end %>
